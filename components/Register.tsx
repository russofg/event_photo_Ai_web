import React from "react";
import { Login } from "./Login";

// For now, we'll just reuse the Login component since we're only using Google Sign-In
export const Register: React.FC = () => {
  return <Login />;
};
